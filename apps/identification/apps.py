from django.apps import AppConfig


class IdentificationConfig(AppConfig):
    """App config for identification reports and findings."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.identification"
