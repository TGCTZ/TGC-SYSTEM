from django.apps import AppConfig


class CertificatesConfig(AppConfig):
    """App config for certificates and public verification."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.certificates"
